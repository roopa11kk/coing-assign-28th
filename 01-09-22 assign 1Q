#include<stdio.h>
#include<conio.h>
char* odd_even(int i);
int main()
{
	int num;
	printf("Enter a number:");
	scanf("%d",&num);
	printf("%s",odd_even(num));
	return 0;
}
char*odd_even(int num)
{
	if(num%2==0)
	{
		return"even";
    }
		else
		{
			return "odd";
		}
	
}
